type SearchResults = {
    results: Array<{
        id: number,
        price: number,
        title: string,
    }>
}


export function SearchResults({ results }) {
    return (
        <div>

        </div>

    );
}